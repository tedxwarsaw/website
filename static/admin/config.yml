backend:
  name: gitlab
  preview_context: preview_status
  repo: tedxwarsaw/tedxwarsaw.gitlab.io
publish_mode: editorial_workflow
media_folder: "static/images/uploads"
public_folder: "/images/uploads"
collections:
  - label: "Pages"
    name: "pages"
    files:
      - label: "Home" # Used in the UI
        name: "home"
        file: "src/content/pages/index.yaml"
        slug: "index.md" # Filename template, e.g., YYYY-MM-DD-title.md
        fields: # The fields for each document, usually in front matter
          - { label: "Title", name: "title", widget: "string" }
  - label: "Global"
    name: "global"
    files:
      - label: "Navbar"
        name: "navbar"
        file: "src/content/global/navbar.yaml"
        fields:
          - label: NavbarPages
            name: navbar_pages
            widget: list
            fields:
              - label: "Display Name",
                name: "display_name",
                widget: "string",
              - { label: "Path", name: "path", widget: "string" }
